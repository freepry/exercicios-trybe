const numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];

for (let index = 0; index < numbers.length; index += 1) {
  // fiz um algoritimo para verificação do numero se ele é impar ou par
  // if (numbers[index] % 2 === 0) {
  //   console.log('É Par: ', numbers[index]);
  // } else {
  //   console.log('É ImPar: ', numbers[index]);
  // }
  console.log(numbers[index]);
};